# 四神兽 / 麒麟 属性表

静态网页，用于查看四神兽与麒麟在不同等级下的属性，并支持按任意属性排序。

## 功能

- 选择等级 1–5 查看对应属性
- 点击表头或下拉菜单按属性排序
- 切换升序 / 降序

## 本地预览

直接用浏览器打开 `index.html`，或在项目目录运行：

```bash
npx serve .
```

## GitHub Pages

推送至 GitHub 后，可在仓库 Settings → Pages 中选择 `main` 分支根目录发布。
